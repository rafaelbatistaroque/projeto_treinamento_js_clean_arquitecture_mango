{
  "name": "projeto_treinamento_js_clean_arquitecture_mango",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "standard": {
    "env": [
      "jest"
    ]
  },
  "devDependencies": {
    "husky": "^4.3.6",
    "jest": "^26.6.3",
    "lint-staged": "^10.5.3",
    "standard": "^16.0.3"
  },
  "scripts": {
    "test": "jest --silent --noStackTrace --passWithNoTests",
    "test:unit": "yarn test --watch -c jest-unit-config.js",
    "test:integration": "yarn test --watch -c jest-integration-config.js",
    "test:staged": "yarn test --findRelatedTests",
    "test:ci": "yarn test --coverage"
  },
  "dependencies": {
    "@types/jest": "^26.0.19",
    "validator": "^13.5.2"
  }
}
